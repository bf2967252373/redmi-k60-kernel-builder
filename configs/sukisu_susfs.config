# =============================================================
# KConfig fragment: SukiSU-Ultra + SUSFS for Redmi K60 (socrates)
# Kernel: 5.10 non-GKI (SM8475 / socrates-t-oss)
#
# This fragment is APPENDED to the vendor defconfig.
# After appending, `make olddefconfig` resolves any conflicts.
# =============================================================

# ---- KernelSU (SukiSU-Ultra) Core ----
CONFIG_KSU=y
# Use manual hook for non-GKI (kprobes not reliable on all 5.10 OEMs)
# CONFIG_KSU_KPROBES_HOOK is not set
CONFIG_KSU_MANUAL_HOOK=y

# ---- SUSFS (SU Filesystem) ----
CONFIG_KSU_SUSFS=y
CONFIG_KSU_SUSFS_SUS_PATH=y
CONFIG_KSU_SUSFS_SUS_MOUNT=y
# NOTE: SUS_MOUNT enables mnt_id_reorder — if you get bootloops, disable:
# CONFIG_KSU_SUSFS_SUS_MOUNT is not set
CONFIG_KSU_SUSFS_AUTO_ADD_SUS_KSU_DEFAULT_MOUNT=y
CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT=y
CONFIG_KSU_SUSFS_SUS_KSTAT=y
CONFIG_KSU_SUSFS_SUS_OVERLAYFS=y
CONFIG_KSU_SUSFS_TRY_UMOUNT=y
CONFIG_KSU_SUSFS_SPOOF_UNAME=y
CONFIG_KSU_SUSFS_ENABLE_LOG=y
# HAS_MAGIC_MOUNT — for newer SukiSU with magic mount support
CONFIG_KSU_SUSFS_HAS_MAGIC_MOUNT=y

# ---- Symbol tables (REQUIRED for non-GKI KSU/KPM) ----
CONFIG_KALLSYMS=y
CONFIG_KALLSYMS_ALL=y

# ---- KPM (Kernel Patch Module) — controlled by ENABLE_KPM env ----
# CONFIG_KPM=y   <- injected dynamically by build step if ENABLE_KPM=true

# ---- Disable debug overhead ----
# CONFIG_DYNAMIC_DEBUG is not set
# CONFIG_DEBUG_KERNEL is not set
